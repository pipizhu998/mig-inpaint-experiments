schema_version: 1
project_root: ../..

experiment:
  name: paper40_g1_g8_g8all12r_512
  output_root: runs
  resume: true

runtime:
  python: $GUARDBENCH_PYTHON

protocol:
  resolution: 512
  attack_seed: 9999
  inpaint_seed: 2000

# Frozen paper-40 manifest from the completed 7.18 experiment.  All four
# methods below consume this one manifest, so numeric IDs cannot silently join
# against the newer 100-image dataset.
dataset:
  root: ../dataset/mig_inpaint_paper40_legacy
  manifest: manifest.json
  image_dir: images
  mask_dir: masks
  id_field: id
  image_field: file
  attack_prompt_field: attack_prompt
  edit_prompts_field: inpaint_prompts
  attack_mask: enlarged_bbox_rho_1.2
  masks:
    segmentation: segmentation.png
    bbox: bbox.png
    enlarged_bbox_rho_1.2: enlarged_bbox_rho_1.2.png
    double_enlarged_bbox_rho_1.44: double_enlarged_bbox_rho_1.44.png
  evaluation_masks:
    - segmentation
    - bbox
    - enlarged_bbox_rho_1.2
    - double_enlarged_bbox_rho_1.44

methods:
  - name: clean
    type: identity

  - name: l2_all_20step_single
    type: advpaint
    params:
      source_root: AdvPaint-main_revised
      model_id: runwayml/stable-diffusion-inpainting
      model_revision: 8a4288a76071f7280aedbdb3253bdb9e9d5d84bb
      model_variant: fp16
      iterations: 250
      linf_pixel: 0.03
      step_size_model: 0.03
      attack_scheduler_steps: 20
      mask_protocol: two_stage
      two_stage_dir: attack_two_stage
      attack_component: all
      layer_match: transformer_blocks
      timestep_indices: "0"
      clean_reference_cache_cpu: false
      loss_saved_tensors_cpu_offload: false
      vae_saved_tensors_cpu_offload: false
      unet_highres_saved_tensors_cpu_offload: false
      autograd_saved_tensors_cpu_offload: false

  - name: cross_concentration_self_l2_down2_mid_up1_multistep
    type: advpaint
    params:
      source_root: AdvPaint-main_revised
      model_id: runwayml/stable-diffusion-inpainting
      model_revision: 8a4288a76071f7280aedbdb3253bdb9e9d5d84bb
      model_variant: fp16
      iterations: 250
      linf_pixel: 0.03
      step_size_model: 0.03
      attack_scheduler_steps: 20
      mask_protocol: two_stage
      two_stage_dir: attack_two_stage
      attack_component: cross_concentration_self_l2_multistep
      layer_match: down_blocks.2,mid_block,up_blocks.1
      timestep_indices: 0,5,10,15,19
      target_word_mode: all
      block_weights: down2:1,mid:1,up1:1
      spatial_entropy_weight: 0.0
      spatial_concentration_weight: 1.0
      spatial_peak_weight: 0.0
      spatial_mass_weight: 0.0
      attn_log_interval: 25
      clean_reference_cache_cpu: false
      loss_saved_tensors_cpu_offload: false
      vae_saved_tensors_cpu_offload: false
      unet_highres_saved_tensors_cpu_offload: false
      autograd_saved_tensors_cpu_offload: false

  - name: g8_all_plus_12resnet_relative_l2
    type: advpaint
    params:
      source_root: AdvPaint-main_revised
      model_id: runwayml/stable-diffusion-inpainting
      model_revision: 8a4288a76071f7280aedbdb3253bdb9e9d5d84bb
      model_variant: fp16
      iterations: 250
      linf_pixel: 0.03
      step_size_model: 0.03
      attack_scheduler_steps: 20
      mask_protocol: two_stage
      two_stage_dir: attack_two_stage
      attack_component: revised_g8_all_losses
      layer_match: down_blocks.2,mid_block,up_blocks.1
      timestep_indices: 0,5,10,15,19
      target_word_mode: all
      block_weights: down2:1,mid:1,up1:1
      spatial_entropy_weight: 0.0
      spatial_concentration_weight: 1.0
      spatial_peak_weight: 0.0
      spatial_mass_weight: 0.0
      attn_log_interval: 25
      clean_reference_cache_cpu: false
      loss_saved_tensors_cpu_offload: false
      vae_saved_tensors_cpu_offload: false
      unet_highres_gradient_checkpointing: true
      unet_highres_saved_tensors_cpu_offload: false
      autograd_saved_tensors_cpu_offload: false

inpainters:
  - name: sd1_inpainting
    type: diffusers_inpaint
    params:
      model_id: runwayml/stable-diffusion-inpainting
      revision: 8a4288a76071f7280aedbdb3253bdb9e9d5d84bb
      variant: fp16
      dtype: float16
      device: cuda
      local_files_only: true
      steps: 50
      guidance_scale: 7.5
      strength: 1.0

evaluators:
  - name: artifact_manifest
    type: manifest
    params: {baseline_method: clean}
  - name: semantic_protection
    type: clip_lpips
    params:
      baseline_method: clean
      metric_source: evaluation_metrics.py
      device: cuda
  - name: masked_pixel
    type: masked_pixel
    params: {baseline_method: clean}
  - name: protected_pixel
    type: protected_pixel
    params: {baseline_method: clean}
